# Web Portfolio

A fresh React application built with Vite.

## Getting started

```bash
npm install
npm run dev
```

Run `npm run build` to create a production build and `npm run lint` to check source files.


# Mascot images

Put the main hero mascot image in this folder as `chad-mascot.png`.

The portfolio reads it from `/mascot/chad-mascot.png`. PNG, WebP, or JPG work; if you use another format, update `mascotSrc` in `src/App.jsx`.
